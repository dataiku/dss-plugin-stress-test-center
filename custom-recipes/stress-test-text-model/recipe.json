{
    "meta": {
        "label": "Corrupt dataset",
        "description": "Apply the perturbations from a selected stress set on a dataset",
        "icon": "icon-bug"
    },
    "selectableFromDataset": "input_dataset",
    "kind": "PYTHON",
    "inputRoles": [
        {
            "name": "input_dataset",
            "label": "Input dataset",
            "description": "The dataset to corrupt" ,
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],
    "outputRoles": [
        {
            "name": "corrupted_dataset",
            "label": "Corrupted dataset",
            "description": "The dataset after applying a stress test perturbation",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],
    "params": [
        {
            "name": "stress_test",
            "label": "Feature perturbation",
            "type": "SELECT",
            "selectChoices": [
                { "value": "MissingValues", "label": "Missing values"},
                { "value": "Scaling", "label": "Scaling"}
            ],
            "mandatory": true
        },
        {
            "name": "selected_features",
            "label": "Corrupted features",
            "type": "COLUMNS",
            "description": "Features where the perturbation will be applied",
            "mandatory": true,
            "columnRole": "input_dataset"
        },
        {
            "name": "samples_fraction",
            "label": "Proportion of samples to corrupt",
            "type": "DOUBLE",
            "defaultValue": 0.5,
            "mandatory": true
        }
    ],
    "resourceKeys": []
}
